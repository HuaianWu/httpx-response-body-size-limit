有些服务会用 HTTPX 拉取第三方接口和文件，偶尔对方配置错误会返回特别大的响应，遇到 gzip 一类压缩内容时实际解压后的体积还可能远大于 Content-Length。现在普通 request 会直接把响应读进内存，容易把进程撑爆。

希望 Client 和 AsyncClient 能配置一个可选的响应体大小上限，单次请求也可以覆盖。超过限制后要尽快停止读取，关闭响应并抛出调用方能够单独处理的异常；有可信长度信息时可以提前失败，但 chunked、错误的 Content-Length 和压缩响应也必须按实际读取结果限制。自动读取、stream 模式、重定向和认证过程中的中间响应都不要绕过限制，同步和异步行为保持一致。

这个能力默认关闭，未配置时保持现有行为。请补上公开 API、文档和不依赖外部网络的测试，尤其确认失败后连接能够正常释放和复用。
